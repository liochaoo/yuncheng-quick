# 统一使用 LF，Windows 脚本保留 CRLF。
* text=auto eol=lf
*.[cC][mM][dD] text eol=crlf
*.[bB][aA][tT] text eol=crlf

# 二进制文件不参与文本转换。
*.ico binary -text
*.png binary -text
*.jpg binary -text
*.jpeg binary -text
*.gif binary -text
*.webp binary -text
*.woff binary -text
*.woff2 binary -text
